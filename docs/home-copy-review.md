# 首頁文案校對紀錄

日期：2026-09-10。範圍：首頁主標題、功能卡、資料流程、圖表操作、五步流程與操作入口。此次只查閱程式與既有文件，未連線儀器，未新增 HIL 證據。

| 原文問題 | 調整及依據 |
|---|---|
| 「工程師真正需要」「可靠量測」偏宣傳，暗示結果保證 | 改為功能名稱及操作流程；可執行不等於有效量測或正式 PASS。 |
| 「任何異常立即停止整批」過度概括 | `workflow/frequency_sweep.py` 在單點返回後檢查關鍵結果、儀器及收尾錯誤，取消也有點位邊界；改為停止後續點位及保留已取得結果。 |
| 「每次 Run」都保存所有檔案 | `web/real_service.py` 依執行路徑產生結果，啟動前拒絕與失敗不保證完整檔案；改以實際紀錄為準。操作文件不宣稱隨每筆結果保存。 |
| 「安全檢查」像是能確認實際接線或保證 RF 關閉 | 後端檢查參數與操作員確認，無法檢查實際線材。收尾會嘗試關閉 RF，發生錯誤仍須現場確認。 |
| 「從頭播放」與行為不符 | `app.js` 僅重設 iframe URL，改名「重新載入圖表」，保留圖內 Play story 說明。 |
| 只有「量測頁」會送 RF | 首頁導覽也包含 Loopback 與 HIL 批次；改述各入口的執行確認。 |
| Path Loss／DUT／UDBox 一律描述為待 HIL | README、SPEC 與累積證據各有適用條件，首頁不概括所有路徑的狀態；改指向目前設定及能力表，不宣稱新增驗證或全面支援。 |
| 中英混雜及程式套件名稱干擾操作說明 | 保留 EVM、SCPI、INVALID 等必要術語；以圖檔匯出、曲線、原始回應、收尾狀態等描述使用者可見功能。 |

主標題與首頁說明的中文 HTML 預設值及中英文 `homeCopy` 同步；架構節點切換語言只更新文字。圖表內部的獨立文件與能力表不屬於本次全面重寫範圍。

驗證採離線／Mock 測試、兩份 YAML 驗證、JavaScript 語法及 `git diff --check`。既有 HIL 僅作能力邊界參考，不作此次量測成果。

亮／暗與中／英文切換補充：首頁狀態卡、能力表標題、欄名、資料列與載入失敗訊息共用語系狀態；切換語言以已讀取的能力資料重繪，不重送 API。切換主題會用相同圖表路徑重新載入嵌入式說明圖並傳入對應主題，不會呼叫量測端點。
